// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"
	"users-service/app/models/ent/blacklistedtoken"
	"users-service/app/models/ent/user"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// BlacklistedToken is the model entity for the BlacklistedToken schema.
type BlacklistedToken struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id"`
	// Token holds the value of the "token" field.
	Token string `json:"token"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at"`
	// ExpiresAt holds the value of the "expires_at" field.
	ExpiresAt time.Time `json:"expires_at"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BlacklistedTokenQuery when eager-loading is set.
	Edges                   BlacklistedTokenEdges `json:"edges"`
	blacklisted_token_owner *int
	selectValues            sql.SelectValues
}

// BlacklistedTokenEdges holds the relations/edges for other nodes in the graph.
type BlacklistedTokenEdges struct {
	// Owner holds the value of the owner edge.
	Owner *User `json:"owner"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// OwnerOrErr returns the Owner value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BlacklistedTokenEdges) OwnerOrErr() (*User, error) {
	if e.Owner != nil {
		return e.Owner, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "owner"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BlacklistedToken) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case blacklistedtoken.FieldID:
			values[i] = new(sql.NullInt64)
		case blacklistedtoken.FieldToken:
			values[i] = new(sql.NullString)
		case blacklistedtoken.FieldCreatedAt, blacklistedtoken.FieldUpdatedAt, blacklistedtoken.FieldExpiresAt:
			values[i] = new(sql.NullTime)
		case blacklistedtoken.ForeignKeys[0]: // blacklisted_token_owner
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BlacklistedToken fields.
func (bt *BlacklistedToken) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case blacklistedtoken.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			bt.ID = int(value.Int64)
		case blacklistedtoken.FieldToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value.Valid {
				bt.Token = value.String
			}
		case blacklistedtoken.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				bt.CreatedAt = value.Time
			}
		case blacklistedtoken.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				bt.UpdatedAt = value.Time
			}
		case blacklistedtoken.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				bt.ExpiresAt = value.Time
			}
		case blacklistedtoken.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field blacklisted_token_owner", value)
			} else if value.Valid {
				bt.blacklisted_token_owner = new(int)
				*bt.blacklisted_token_owner = int(value.Int64)
			}
		default:
			bt.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BlacklistedToken.
// This includes values selected through modifiers, order, etc.
func (bt *BlacklistedToken) Value(name string) (ent.Value, error) {
	return bt.selectValues.Get(name)
}

// QueryOwner queries the "owner" edge of the BlacklistedToken entity.
func (bt *BlacklistedToken) QueryOwner() *UserQuery {
	return NewBlacklistedTokenClient(bt.config).QueryOwner(bt)
}

// Update returns a builder for updating this BlacklistedToken.
// Note that you need to call BlacklistedToken.Unwrap() before calling this method if this BlacklistedToken
// was returned from a transaction, and the transaction was committed or rolled back.
func (bt *BlacklistedToken) Update() *BlacklistedTokenUpdateOne {
	return NewBlacklistedTokenClient(bt.config).UpdateOne(bt)
}

// Unwrap unwraps the BlacklistedToken entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (bt *BlacklistedToken) Unwrap() *BlacklistedToken {
	_tx, ok := bt.config.driver.(*txDriver)
	if !ok {
		panic("ent: BlacklistedToken is not a transactional entity")
	}
	bt.config.driver = _tx.drv
	return bt
}

// String implements the fmt.Stringer.
func (bt *BlacklistedToken) String() string {
	var builder strings.Builder
	builder.WriteString("BlacklistedToken(")
	builder.WriteString(fmt.Sprintf("id=%v, ", bt.ID))
	builder.WriteString("token=")
	builder.WriteString(bt.Token)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(bt.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(bt.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("expires_at=")
	builder.WriteString(bt.ExpiresAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// BlacklistedTokens is a parsable slice of BlacklistedToken.
type BlacklistedTokens []*BlacklistedToken
