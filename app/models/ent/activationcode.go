// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"
	"users-service/app/models/ent/activationcode"
	"users-service/app/models/ent/user"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ActivationCode is the model entity for the ActivationCode schema.
type ActivationCode struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id"`
	// Code holds the value of the "code" field.
	Code string `json:"code"`
	// Used holds the value of the "used" field.
	Used bool `json:"used"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at"`
	// ExpiresAt holds the value of the "expires_at" field.
	ExpiresAt time.Time `json:"expires_at"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ActivationCodeQuery when eager-loading is set.
	Edges                ActivationCodeEdges `json:"edges"`
	user_activation_code *int
	selectValues         sql.SelectValues
}

// ActivationCodeEdges holds the relations/edges for other nodes in the graph.
type ActivationCodeEdges struct {
	// Owner holds the value of the owner edge.
	Owner *User `json:"owner"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// OwnerOrErr returns the Owner value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ActivationCodeEdges) OwnerOrErr() (*User, error) {
	if e.Owner != nil {
		return e.Owner, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "owner"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ActivationCode) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case activationcode.FieldUsed:
			values[i] = new(sql.NullBool)
		case activationcode.FieldID:
			values[i] = new(sql.NullInt64)
		case activationcode.FieldCode:
			values[i] = new(sql.NullString)
		case activationcode.FieldCreatedAt, activationcode.FieldUpdatedAt, activationcode.FieldExpiresAt:
			values[i] = new(sql.NullTime)
		case activationcode.ForeignKeys[0]: // user_activation_code
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ActivationCode fields.
func (ac *ActivationCode) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case activationcode.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ac.ID = int(value.Int64)
		case activationcode.FieldCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code", values[i])
			} else if value.Valid {
				ac.Code = value.String
			}
		case activationcode.FieldUsed:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field used", values[i])
			} else if value.Valid {
				ac.Used = value.Bool
			}
		case activationcode.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ac.CreatedAt = value.Time
			}
		case activationcode.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				ac.UpdatedAt = value.Time
			}
		case activationcode.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				ac.ExpiresAt = value.Time
			}
		case activationcode.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_activation_code", value)
			} else if value.Valid {
				ac.user_activation_code = new(int)
				*ac.user_activation_code = int(value.Int64)
			}
		default:
			ac.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ActivationCode.
// This includes values selected through modifiers, order, etc.
func (ac *ActivationCode) Value(name string) (ent.Value, error) {
	return ac.selectValues.Get(name)
}

// QueryOwner queries the "owner" edge of the ActivationCode entity.
func (ac *ActivationCode) QueryOwner() *UserQuery {
	return NewActivationCodeClient(ac.config).QueryOwner(ac)
}

// Update returns a builder for updating this ActivationCode.
// Note that you need to call ActivationCode.Unwrap() before calling this method if this ActivationCode
// was returned from a transaction, and the transaction was committed or rolled back.
func (ac *ActivationCode) Update() *ActivationCodeUpdateOne {
	return NewActivationCodeClient(ac.config).UpdateOne(ac)
}

// Unwrap unwraps the ActivationCode entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ac *ActivationCode) Unwrap() *ActivationCode {
	_tx, ok := ac.config.driver.(*txDriver)
	if !ok {
		panic("ent: ActivationCode is not a transactional entity")
	}
	ac.config.driver = _tx.drv
	return ac
}

// String implements the fmt.Stringer.
func (ac *ActivationCode) String() string {
	var builder strings.Builder
	builder.WriteString("ActivationCode(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ac.ID))
	builder.WriteString("code=")
	builder.WriteString(ac.Code)
	builder.WriteString(", ")
	builder.WriteString("used=")
	builder.WriteString(fmt.Sprintf("%v", ac.Used))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ac.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(ac.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("expires_at=")
	builder.WriteString(ac.ExpiresAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ActivationCodes is a parsable slice of ActivationCode.
type ActivationCodes []*ActivationCode
